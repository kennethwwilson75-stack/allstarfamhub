{
  "name": "@allstarfamhub/mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'mobile build placeholder — set up in Step 8'",
    "dev": "echo 'mobile dev placeholder — set up in Step 8'",
    "lint": "echo 'mobile lint placeholder — set up in Step 8'",
    "typecheck": "echo 'mobile typecheck placeholder — set up in Step 8'"
  },
  "dependencies": {
    "@allstarfamhub/shared": "workspace:*"
  }
}
